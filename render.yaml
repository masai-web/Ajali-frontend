services:
  - type: web
    name: ajali-frontend
    plan: free
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npx serve -s dist  # Use npx to run serve without global install
    envVars:
      - key: VITE_API_URL
        value: https://ajali-backend-7ex3.onrender.com